language: python
python:
  - "2.7"
  - "3.6"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libicu-dev
install:
  - pip install -e .
  - pip install coverage nose pyicu
script:
  - nosetests --with-coverage --cover-package=dalet --cover-erase